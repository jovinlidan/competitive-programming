#include <bits/stdc++.h>
#define lli long long int
using namespace std;
int main()
{
 int x, y;
 cin >> x >> y;
 if(x-y  == 0 || x-y == 1) cout << x << endl;
 else cout << 2 << endl;
 
 
 
 
 
 
 
 
 return 0;
}